// Web chat widget (embeddable) — Phase 1 เป็น placeholder
// Phase 2 จะเติม: แชท UI, session bootstrap, ส่ง inbound ไป apps/api, WS reconnect รับ outbound

export function mountWidget(target: HTMLElement): void {
  target.textContent = 'Omni widget placeholder — Phase 2 จะเป็นแชทจริง';
}
